export function calculateAffiliateCommission(sellingPrice: number): number {
  return sellingPrice * 0.1
}

export function calculateInvestmentReserve(sellingPrice: number): number {
  return sellingPrice * 0.15
}
